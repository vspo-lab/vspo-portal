---
interface Props {
  class?: string;
  style?: string;
}

const { class: className = "", style } = Astro.props;
---

<div
  class={`rounded-2xl bg-surface-container text-on-surface shadow-card ${className}`}
  style={style}
>
  <slot />
</div>
