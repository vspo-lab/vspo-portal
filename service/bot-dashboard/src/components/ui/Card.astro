---
interface Props {
  class?: string;
  style?: string;
}

const { class: className = "", style } = Astro.props;
---

<div
  class={`rounded-[--radius] border border-border bg-card text-card-foreground shadow-card ${className}`}
  style={style}
>
  <slot />
</div>
