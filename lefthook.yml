pre-commit:
  parallel: true
  commands:
    type-check:
      run: pnpm tsc
    biome:
      run: pnpm biome:check
    knip:
      run: pnpm knip
    textlint:
      glob: "*.md"
      run: pnpm textlint
    markdownlint:
      glob: "*.md"
      run: pnpm markdownlint
